<header style="display:flex; justify-content:space-between; align-items:center; padding:15px 40px; background:#eee;">

  <!-- LEFT -->
  <div>
    <h2>ZIYAURA</h2>
  </div>

  <!-- CENTER -->
  <nav style="display:flex; gap:20px;">
    <a href="/">HOME</a>
    <a href="#">CATEGORIES</a>
    <a href="#">LIMITED EDITION</a>
    <a href="#">NEED HELP</a>
  </nav>

  <!-- RIGHT -->
  <div style="display:flex; gap:20px; align-items:center;">

    <span>♡</span>
    <span>🛒</span>
    <span>🔍</span>

    <% if (!user) { %>
      <a href="/login">SIGN IN</a>
    <% } else { %>
      <span><%= user.name %></span>
      <a href="/logout">LOGOUT</a>
    <% } %>

  </div>

</header>